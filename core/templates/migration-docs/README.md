# migration-docs

Drop **legacy PDF or Word** course files here. Convert them to markdown in
`lectures/`, `assignments/`, or `docs/` with the instructor skill
`migrate-course`.

Source dumps stay **local**. Git ignores everything in this folder except
this README. Do not commit restricted extracts, PHI, secrets, or answer keys.
